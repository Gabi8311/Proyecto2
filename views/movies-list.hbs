<div class="container">
    <div class="row">
        {{#each allMovies}}
        <div class="col-3"></div>
            <img src="{{coverImg}}" alt="cover image">
            <h2 class="registro text-center">{{title}}</h2>
            <a href="/movies/{{_id}}" class="btn btn-sm btn-danger text-warning ml-5">Ver detalle</a>
        </div>
        {{/each}}
    </div>

</div>